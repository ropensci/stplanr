language: r
cache: packages
####### sfr-dep - Original SF branch, superseded by updated base branch

#apt_packages:
#  - libudunits2-dev

#before_install:
#  - export CURWD=`pwd`
#  - sudo apt-get install libproj-dev libgeos++-dev
#  - cd
#  - wget http://download.osgeo.org/gdal/2.1.0/gdal-2.1.0.tar.gz
#  - tar zxf gdal-2.1.0.tar.gz
#  - cd gdal-2.1.0
#  - ./configure
#  - make
#  - sudo make install
#  - cd
#  - wget http://download.osgeo.org/proj/proj-4.9.3.tar.gz
#  - tar zxvf proj-4.9.3.tar.gz
#  - cd proj-4.9.3
#  - ./configure
#  - make
#  - sudo make install
#  - sudo ldconfig
#  - cd $CURWD

#sudo: required

#r_github_packages: edzer/sfr

#r_packages:
#  - rgdal
#  - roxygen2
#  - rmarkdown
#=======
dist: trusty
sudo: required
before_install:
  - sudo add-apt-repository ppa:ubuntugis/ubuntugis-unstable --yes
  - sudo apt-get --yes --force-yes update -qq
  - sudo apt-get install --yes libudunits2-dev libproj-dev libgeos-dev libgdal-dev libv8-dev

r_packages:
  - rgdal
  - sf
  - devtools

after_success:
- Rscript -e 'covr::codecov()'
#>>>>>>> sfr

notifications:
    email:
      on_success: change
      on_failure: change
