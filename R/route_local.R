# TODO: implement with sfnetworks
# route_local <- function(sln, from, to, l = NULL, ...) {
#   if(is.null(l)) {
#     coords <- od_coords(from, to)
#   } else {
#     coords <- od_coords(l)
#   }
# }
